<!DOCTYPE html>
html(lang="en")
head
	meta(charset="UTF-8")
	title Plataforma
body
	header
		h2 Logo Ascent
		div
			img(src="#{user.photo.path}", width="50")
			//- p #{user.email}
			p @ #{user.nickname}
			div
				a(href="/plataforma/perfil/#{user._id}") Perfil
			div
				a(href="/logout") Salir
			div
				p Tu eres User #{user.access}
	section
		p #{msg}
		h2 ¿Eliminar este Curso?
		
		img(src="#{curso.icon.path}", width="60")
		h3 #{curso.title}
		div
			p Slogan: #{curso.slogan}
			p Promedio Rate: #{curso.promedioRate}
			p Id base de datos: #{curso._id}
			p Fecha de creación: #{curso.createdAt}


		form(action="/plataforma/admin/cursos/delete/#{curso._id}?_method=delete", method="post")
			p Escribe el nombre del curso para confirmar:
			div
				label Nombre del curso
				input(type="text", name="curso_confirmar", target="_blank")

			button Confirmar Eliminar