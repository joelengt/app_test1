<!DOCTYPE html>
html(lang="en")
head
	meta(charset="UTF-8")
	meta(name="viewport", content="width=device-width, minimum-scale=1, maximum-scale=1")
	title Ascent: Admin Dashboard
body
	header
		h2 Logo Ascent
		p Admin Dashboard
	div
		img(src="#{user.photo.path}", width="50")
		//- p #{user.email}
		p @ #{user.nickname}	
		div
			a(href="/plataforma/perfil/#{user._id}") Perfil
		div
			a(href="/logout") Salir
		div
			p Tu eres User #{user.access}
	div
		p CREAR PREGUNTA: Cursos

		form(action="/plataforma/admin/preguntas/diplomados/add", method="post")
			div
				label ¿Qué tipo de preguntas quieres crear?
				div
					input(type="radio", name="pregunta_tipos", value="alternativa")
					label alternativa
				div
					input(type="radio", name="pregunta_tipos", value="completar")
					label completar
				div
					input(type="radio", name="pregunta_tipos", value="imagenes")
					label imagenes
				div
					input(type="radio", name="pregunta_tipos", value="escribir")
					label escribir

			button Crear
